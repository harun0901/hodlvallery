import Head from 'next/head';
import { SwapTokens } from "../modals";

const SwapPage: React.FC = () => (
  <>
    <Head>
      <title>HODL Valley - SWAP</title>
    </Head>
    <SwapTokens />
  </>
)
export default SwapPage;

